name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  test:
    name: Test (Node ${{ matrix.node-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [8, 9, 10, 12, 14, 16, 18, 20, 22, 24]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install i2c-bus (skip native build)
        run: npm install i2c-bus --ignore-scripts --legacy-peer-deps

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Test
        run: npm test

      - name: Report coverage
        run: npm run report-coverage
        env:
          COVERALLS_REPO_TOKEN: ${{ secrets.COVERALLS_REPO_TOKEN }}

      - name: Build
        run: npm run build

      - name: Build docs
        run: npm run build-docs


  release:
    name: Release
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master' && github.event_name == 'push'
    permissions:
      id-token: write # Required for OIDC / Trusted Publishing
      contents: write  # Required for semantic-release to push tags
      issues: write
      pull-requests: write


    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'npm'

      - name: Install i2c-bus (skip native build)
        run: npm install i2c-bus --ignore-scripts --legacy-peer-deps

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Prune
        run: npm prune --legacy-peer-deps

      - name: Build
        run: npm run build

      - name: Build docs
        run: npm run build-docs

      - name: Semantic Release
        run: npx semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: docs/motor-hat/1.0.0-semanticrelease
